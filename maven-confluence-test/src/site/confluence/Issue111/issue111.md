

## inline

inline `java -cp {classpaths} {mainClass} {inputPath} {outputPath}` inline

\\\\    
    
## code block

```
java -cp {classpaths} {mainClass} {inputPath} {outputPath}
```

\\\\    

##  escape

java -cp \\{classpaths\} \\{mainClass} \\{inputPath} \\{outputPath}

